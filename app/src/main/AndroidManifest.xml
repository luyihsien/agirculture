<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="tw.edu.ntu.app">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
    </queries>

    <application
        android:name=".GlobalVariable"
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@drawable/agritraceabilityapp_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/agritraceabilityapp_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.App"
        tools:ignore="ExtraText">

        <receiver
            android:name=".CalenderAlarmReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="CALENDAR_ALARM_RECEIVER"></action>
            </intent-filter>
        </receiver>
        <receiver
            android:name=".BootUpReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"></action>
            </intent-filter>
        </receiver>

        <activity
            android:name=".FlowerActivity"
            android:exported="false"
            android:parentActivityName=".MethodActivity" />
        <activity android:name=".CalendarActivity" />
        <activity android:name=".CorrectActivity" />
        <activity android:name=".QRCodeActivity" />
        <activity android:name=".KeyboardActivity" />
        <activity android:name=".HearingActivity" />
        <activity android:name=".MethodActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.journeyapps.barcodescanner.CaptureActivity"
            android:screenOrientation="fullSensor"
            tools:replace="screenOrientation" /> <!-- <meta-data android:name="web_api_url" android:value="https://kh8.tw/"/> -->
        <meta-data
            android:name="web_api_url"
            android:value="https://www.test-app.ntu.asia/" />
    </application>

</manifest>
<!--android:value="https://www.ricehouse-app.ntu.asia/" /-->